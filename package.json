{
  "name": "cgfp",
  "version": "1.0.0",
  "description": "Lists the dependency and dev dependency in the project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist/",
    "build": "tsc",
    "start": "tsc && node dist/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-local": "tsc && (npm install -g) && cgfp"
  },
  "bin": {
    "cgfp": "./dist/cli.js"
  },
  "author": "Chirag",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.5.1",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {}
}
